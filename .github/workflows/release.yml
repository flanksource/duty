name: Create Release
on:
  push:
    branches:
      - main
jobs:
  semantic-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: codfish/semantic-release-action@v2
        id: semantic
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  bump-clients:
    runs-on: ubuntu-latest
    needs: [semantic-release]
    if: ${{ jobs.semantic-release.steps.semantic.outputs.new-release-published == 'true' }}
    strategy:
      matrix:
        repos: ['incident-commander', 'canary-checker', 'config-db']
    steps:
      - uses: actions/checkout@v3
        with:
          repository: flanksource/${{ matrix.repos }}
      - name: Create commits
        run: |
          git checkout -b bump-duty 
          # Sleep to let index to refresh
          sleep 60
          go get -u github.com/flanksource/duty@${{ jobs.semantic-release.steps.semantic.outputs.release-version }}
      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v4
        with:
          branch: 'bump-duty-@{{ jobs.semantic-release.steps.semantic.outputs.release-version }}'
          commit-message: 'chore: bump duty'
          title: 'chore: bump duty'
          commiter: 'Flankbot <flankbot@users.noreply.github.com>'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
