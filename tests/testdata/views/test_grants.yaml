apiVersion: mission-control.flanksource.com/v1
kind: View
metadata:
  name: test_grants
  namespace: mc
spec:
  columns:
    - name: id
      type: string
      primaryKey: true
    - name: name
      type: string
    - name: type
      type: string
  queries:
    configs:
      configs:
        name: '*'
